import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AgentRunsPage() {
  return (
    <PlaceholderPage
      title="Agent Runs"
      phase="Phase 7"
      summary="Agent run history will show status, latency, model, cost, structured steps, retrieved chunks, tool calls, and errors."
    />
  );
}

